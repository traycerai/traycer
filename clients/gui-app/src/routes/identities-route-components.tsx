/**
 * The `/identities/$identityId` route renders nothing of its own: the
 * identity surface is a top-level tab surface, mounted by `TopLevelTabHost`
 * for the `identity` tab the navigation controller resolves this location to.
 */
export function IdentityRoute() {
  return null;
}
