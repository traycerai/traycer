/** The rationale below lived in both of them, byte-identical, and ended with "revisit at the next Vite bump". */

export const ZOD_INLINE_SERVER_DEPS = {
  deps: { inline: [/[\\/]node_modules[\\/]zod[\\/]/] },
} as const;
