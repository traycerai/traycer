{
  "comment": "Dev CLI wrapper filenames staged by `make dev-desktop` (scripts/dev-desktop.js) and consumed by desktop CLI discovery + host-management IPC. Multi-run dev computes the root from DEV_DESKTOP_SLOT (`~/.traycer/cli/dev-runs/<slot>/bin/`); segments are the legacy no-slot dev layout.",
  "segments": [".traycer", "cli", "dev", "bin"],
  "filenamePosix": "traycer",
  "filenameWin32": "traycer.cmd"
}
