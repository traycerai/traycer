{
  "name": "@traycer-clients/mobile",
  "version": "0.0.0",
  "description": "Capacitor mobile runner for Traycer gui-app. Thin native shell that consumes gui-app as a library and bridges Capacitor plugins to the shared IRunnerHost contract.",
  "type": "module",
  "private": true,
  "scripts": {
    "compile": "tsc -b",
    "build:web": "vite build --config vite.config.ts",
    "build:web:staging": "TRAYCER_MOBILE_ENV=staging vite build --config vite.config.ts",
    "dev:web": "vite --config vite.config.ts",
    "dev:ios": "bun scripts/dev-ios.ts",
    "dev:ios:device": "bun scripts/dev-ios-device.ts",
    "sync:ios": "bun run build:web && bun x cap sync ios",
    "sync:ios:staging": "bun run build:web:staging && bun x cap sync ios",
    "open:ios": "bun x cap open ios",
    "build:ios": "bun run sync:ios && bun x cap build ios",
    "dev:android": "bun scripts/dev-android.ts",
    "sync:android": "bun run build:web && bun x cap sync android",
    "open:android": "bun x cap open android",
    "build:android": "bun run sync:android && bun x cap build android",
    "lint": "eslint . --cache --fix --max-warnings 0",
    "format": "bun x prettier --write .",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts"
  },
  "dependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/app": "8.1.1",
    "@capacitor/app-launcher": "8.0.1",
    "@capacitor/barcode-scanner": "3.1.0",
    "@capacitor/core": "8.4.2",
    "@capacitor/device": "8.0.0",
    "@capacitor/ios": "8.4.2",
    "@capacitor/keyboard": "8.0.5",
    "@capacitor/push-notifications": "8.1.2",
    "capacitor-native-settings": "8.2.0",
    "capacitor-secure-storage-plugin": "0.13.0",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2",
    "@rolldown/plugin-babel": "catalog:",
    "@tailwindcss/vite": "catalog:",
    "@tanstack/router-plugin": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@typescript-eslint/eslint-plugin": "catalog:",
    "@typescript-eslint/parser": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
